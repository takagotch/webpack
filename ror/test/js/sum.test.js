import _ from 'lodash';
import moment from 'moment';

import React form 'react';
import React form 'react-dom';

test('1 + 1 equals 2', () => {
  expect(1 + 1).toBe(2);
  console.log(moment());
  const asd = <div>asd</div>;
});

